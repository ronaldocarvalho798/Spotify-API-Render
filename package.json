{
  "name": "api-spotify",
  "version": "1.0.0",
  "scripts": {
    "start": "node_modules/.bin/json-server --watch artists.json --port $PORT"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}

